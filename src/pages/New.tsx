import React from "react";
import ParkCleanupMessage from "../components/ParkCleanupMessage";

const New: React.FC = () => {
  return (
    <div className="bg-neutral-900 min-h-screen flex items-center justify-center">
      <ParkCleanupMessage />
    </div>
  );
};

export default New;
